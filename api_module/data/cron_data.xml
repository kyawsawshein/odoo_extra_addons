<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Cron job for automatic contact sync -->
        <!-- <record id="cron_sync_contacts" model="ir.cron">
            <field name="name">API Module: Sync Contacts</field>
            <field name="model_id" ref="model_api_config"/>
            <field name="state">code</field>
            <field name="code">model._cron_sync_contacts()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">hours</field>
            <field name="numbercall">-1</field>
            <field name="doall" eval="False"/>
            <field name="active" eval="True"/>
        </record> -->

        <!-- Cron job for automatic product sync -->
        <!-- <record id="cron_sync_products" model="ir.cron">
            <field name="name">API Module: Sync Products</field>
            <field name="model_id" ref="model_api_config"/>
            <field name="state">code</field>
            <field name="code">model._cron_sync_products()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">hours</field>
            <field name="numbercall">-1</field>
            <field name="doall" eval="False"/>
            <field name="active" eval="True"/>
        </record> -->

        <!-- Cron job for automatic all data sync -->
        <!-- <record id="cron_sync_all_data" model="ir.cron">
            <field name="name">API Module: Sync All Data</field>
            <field name="model_id" ref="model_api_config"/>
            <field name="state">code</field>
            <field name="code">model._cron_sync_all_data()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">hours</field>
            <field name="numbercall">-1</field>
            <field name="doall" eval="False"/>
            <field name="active" eval="True"/>
        </record> -->
    </data>
</odoo>