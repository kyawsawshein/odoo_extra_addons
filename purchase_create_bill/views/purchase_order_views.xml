<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- Inherit Purchase Order Form View -->
        <record id="view_purchase_order_form_inherit_create_bill" model="ir.ui.view">
            <field name="name">purchase.order.form.inherit.create.bill</field>
            <field name="model">purchase.order</field>
            <field name="inherit_id" ref="purchase.purchase_order_form"/>
            <field name="arch" type="xml">

                <!-- Add Create Bill button in header -->
                <button name="button_confirm" position="after">
                    <button name="action_create_vendor_bill" type="object" string="Create Bill" class="btn-primary" invisible="state not in ('purchase', 'done')" help="Create a vendor bill for this purchase order"/>
                </button>

            </field>
        </record>

    </data>
</odoo>